% arara: lualatex: { shell: true}
% arara: lualatex: { shell: true}

\providecommand{\toplevel}{../../../..}
\providecommand{\importPath}{\toplevel/Shared/Imports}
\providecommand{\assetPath}{\toplevel/Assets}
\providecommand{\sharedPath}{\toplevel/Shared}

\documentclass[tikz, hidelinks, float=false, crop=false]{standalone}

\input{\importPath/preamble}
\input{\importPath/glossary}
\input{\importPath/symbols}

\begin{document}
    \begin{tikzpicture}
        \begin{axis}[ymode=log,
            grid=both,
            ymin=1e-6,
            ymax=1e3,
            max space between ticks=20,
            xmin=0,
            xmax=350,
            xlabel=\gls{ms},
            ylabel=\gls{mg},]
            \addplot[color=red, mark=*, mark size=0.4, style=thick]table[x=time, y=p1, col sep=comma]{\assetPath/Data/vacuumTech.csv};
            \addplot[color=black, mark=*, mark size=0.4, style=thick]table[x=time, y=p2, col sep=comma]{\assetPath/Data/vacuumTech.csv};
        \end{axis}
    \end{tikzpicture}
\end{document}
